# 變更紀錄

版本號記在 `project.godot` 的 `config/version`。
發佈時 `tools/publish_pages.sh` 會把 source commit 的短 SHA 寫進
gh-pages 的 commit 訊息與 `version.txt`，線上版因此對得回一個 commit。

## 0.2.0

一次體檢之後的全面修正。

### 修正

- **水管暗房會清空整局成績。** `_load_level` 以前用 `not _in_room` 推導
  「要不要重建 RunStats」，但 `_return_to_level` 與 `_respawn` 都在呼叫它
  之前就把旗標設回 false。實際後果：從暗房走回主關卡分數金幣歸零、生命
  補滿；在暗房裡死掉不用扣命（無限命）；檢查點被關卡起點蓋掉；換場走
  `respawn_at` 讓大牛在進水管瞬間變回小牛。
- **關卡檔一個字元有誤會進入無限墜落死局。** 解析失敗只 `push_error` 就
  return，`_map` 維持 null，而掉出關卡的死亡判定有 `_map == null` 早退——
  主角無限下墜且永遠不判死，要等 300 秒計時歸零。
- **關卡裡有一個盲跳致死點。** 磚台右緣懸在坑上方，而玩家站在那個高度時
  相機被夾住、關卡底部完全在畫面外，落點容錯只剩 19 px。
- **暗房的牛奶磚幾何上永遠頂不到**（正下方緊貼一塊實心磚）。
- 丟出的金幣會穿過問號磚、水管與移動平台。
- 死掉的敵人在 0.22 秒淡出期間還會傷人。
- 選角按空白鍵確認會被同一幀的物理步當成跳躍。
- 空中受傷時無敵閃爍會凍結，玩家看不出自己還在無敵中。
- 桌面版複製成績明明成功卻顯示「複製失敗」。
- 敵人沒有終端速度也沒有掉出關卡的清除，會無限下墜且永不釋放。
- 箭頭俯衝與 Boss 投擲的零向量 `normalized()` 會讓它們停在半空中抖動。
- 圖磚貼圖載入失敗會讓整個 `_ready()` 崩掉、停在全黑畫面。
- 開機時整張關卡被建了兩次。

### 新增

- 音效與背景音樂。以前一個位元組都沒有。18 個 8-bit 音效加一段循環 BGM，
  由 `tools/prepare_audio.py` 用純標準函式庫合成。
- 暫停選單（ESC／P／Start），含音效與音樂音量。
- 最高分存檔，標題畫面顯示。
- 手把支援；↑ 也能跳；行動裝置的虛擬按鍵（多點觸控）。
- 死亡表演、Boss 血條、得分浮字。
- 三隻主角的手感差異（衝刺速度、土狼時間、初始金幣）。
- 每 5000 分加一條命。

### 變更

- **衝刺與丟金幣分家**：衝刺是 Shift、丟金幣是 J。共用一個鍵是先前刻意的
  取捨，但代價比預期大——金幣是有限彈藥、Boss 要六發，習慣按住 Shift 跑的
  玩家會在抵達關底前把彈藥灑光而不知道是自己造成的。
- **丟出的金幣會退掉撿到時記的 50 分**。以前不退分，「留著換分還是拿去
  打怪」的取捨根本不成立，最優解永遠是「撿到就丟」。
- 關卡檔一律要有 `---` 分界線。`#` 在中繼資料區是註解、在地圖區是地面磚，
  沒有分界線就無從分辨。
- 字型子集化：5.57 MB → 0.14 MB。

### 工程

- 加上 CI（`.github/workflows/verify.yml`）。
- `run_tests.sh` 現在會擋下「一個測試都沒跑到」與「有 pending」。
- `verify_game.sh` 有逾時保護，並且整合測試場景檔缺了就算失敗。
- `publish_pages.sh` 發佈前跑完整驗證、拒絕從 dirty working tree 發佈、
  匯出前清空目錄、檢查 pck 而不只是 wasm、把 source SHA 寫進產物。
- Godot 執行檔路徑不再硬編碼在個人家目錄（`tools/godot_env.sh`）。
- 加上 LICENSE、字型的 OFL 全文、`tools/requirements.txt`、`.gitattributes`。
- `build/` 加上 `.gdignore`，並補進 `exclude_filter`——以前上一次發佈的
  icon 會被當成專案資源匯入，再打包進下一次的 PCK，每發佈一次膨脹一點。

### 測試

155 → 227 單元測試，64 → 100 整合檢查。
